allowBuilds:
  '@tsparticles/engine': true
  esbuild: true

# Reached transitively at a version with a known advisory.
overrides:
  lodash-es: '>=4.18.0'

auditConfig:
  # react-router RSC-mode advisory. This app mounts a plain BrowserRouter,
  # so the affected code path is never loaded.
  ignoreGhsas:
    - GHSA-qwww-vcr4-c8h2
