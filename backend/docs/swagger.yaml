info:
  contact: {}
  description: The internal REST API for managing the GoAway DNS sinkhole and its
    settings.
  title: GoAway Backend API
  version: "1.0"
paths: {}
securityDefinitions:
  BearerAuth:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
